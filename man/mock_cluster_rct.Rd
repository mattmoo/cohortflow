% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock-designs.R
\name{mock_cluster_rct}
\alias{mock_cluster_rct}
\title{Generate synthetic data for a cluster-randomised controlled trial}
\usage{
mock_cluster_rct(
  n_clusters = 12L,
  n_participants = 480L,
  n_sites = 3L,
  arm_labels = c("Control", "Intervention"),
  seed = 123L
)
}
\arguments{
\item{n_clusters}{Integer. Number of clusters.}

\item{n_participants}{Integer. Total number of participants, distributed
unevenly across clusters.}

\item{n_sites}{Integer. Number of sites (clusters nested in sites; must
be <= \code{n_clusters}).}

\item{arm_labels}{Character vector. Labels for the trial arms; clusters
are allocated to these labels with balanced randomisation.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per participant, including
\code{participant_id}, \code{cluster_id}, \code{site_id}, \code{arm} (cluster-level),
demographic columns, and consent-flow columns.
}
\description{
Produces one row per participant, nested within clusters (e.g. GP
practices, schools) that are themselves nested within sites. Each
cluster -- not each participant -- is randomised to an arm, so all
participants within a cluster share the same \code{arm}.
}
\examples{
mock_cluster_rct()
mock_cluster_rct(n_clusters = 20, n_participants = 800, seed = 5)
}
