% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{randomise}
\alias{randomise}
\title{Mark the randomisation/allocation point in a criteria pipeline}
\usage{
randomise(criteria, by, arms, label = "Randomised", category = NULL)
}
\arguments{
\item{criteria}{A \code{cf_criteria} object (or \code{NULL} to start a new one).}

\item{by}{One or more column names (character vector) naming the
randomisation unit (e.g. \code{"cluster_id"} for cluster randomisation, or
the participant id for individual randomisation).}

\item{arms}{A single column name (string) holding each unit's allocated
arm (e.g. \code{"arm"}).}

\item{label}{A short human-readable description of this step. Default
\code{"Randomised"} (unlike other builders, a label is not required).}

\item{category}{An optional string grouping this step with others for
display. \code{NULL} leaves it uncategorised.}
}
\value{
The updated \code{cf_criteria} object.
}
\description{
\code{randomise()} records where allocation happens in the pipeline without
excluding any rows (\code{n_fail} is always \code{0}). \code{\link[=as_consort_diagram]{as_consort_diagram()}} and
\code{\link[=as_attrition_tibble]{as_attrition_tibble()}} use its \code{by}/\code{arms} to default \code{branch_by} when
not explicitly supplied, and steps at or after it are flagged
\code{post_randomisation} in \code{\link[=as_attrition_tibble]{as_attrition_tibble()}}'s output.
}
\examples{
cf_criteria() |>
  include(~ eligible, label = "Eligible clusters") |>
  randomise(by = "cluster_id", arms = "arm") |>
  exclude(~ withdrew, label = "Withdrew after allocation")
}
