% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock-designs.R
\name{mock_parallel_rct}
\alias{mock_parallel_rct}
\title{Generate synthetic data for a parallel-group randomised controlled trial}
\usage{
mock_parallel_rct(
  n_participants = 300L,
  arm_labels = c("Control", "Intervention"),
  seed = 123L
)
}
\arguments{
\item{n_participants}{Integer. Total number of participants.}

\item{arm_labels}{Character vector. Labels for the trial arms (allocation
is balanced across these labels).}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per participant, including
\code{participant_id}, \code{arm}, demographic columns (\code{age}, \code{age_group},
\code{sex}, \code{ethnicity}), and consent-flow columns (\code{eligible_screen},
\code{consent_date}, \code{baseline_complete}, \code{withdrew}).
}
\description{
Produces one row per participant, individually randomised to one of
\code{length(arm_labels)} arms with equal allocation. No clustering or
repeated measures -- the simplest of the four designs.
}
\examples{
mock_parallel_rct()
mock_parallel_rct(n_participants = 300, arm_labels = c("Placebo", "Drug"), seed = 7)
}
