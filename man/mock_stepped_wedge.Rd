% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock-designs.R
\name{mock_stepped_wedge}
\alias{mock_stepped_wedge}
\title{Generate synthetic data for a stepped-wedge cluster-randomised trial}
\usage{
mock_stepped_wedge(
  n_clusters = 12L,
  n_participants = 480L,
  n_periods = 4L,
  n_sites = 3L,
  arm_labels = c("Control", "Intervention"),
  seed = 123L
)
}
\arguments{
\item{n_clusters}{Integer. Number of clusters.}

\item{n_participants}{Integer. Total number of participants (each
contributes one row per period), distributed unevenly across clusters.}

\item{n_periods}{Integer. Number of periods, including the initial
all-control period. Must be >= 2 (clusters need at least one period
before crossover and one after).}

\item{n_sites}{Integer. Number of sites (clusters nested in sites; must
be <= \code{n_clusters}).}

\item{arm_labels}{Character vector of length 2: \code{c(control_label, intervention_label)}.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per participant per period,
including \code{participant_id}, \code{event_id}, \code{cluster_id}, \code{site_id},
\code{period}, \code{sequence}, \code{arm} (derived from \code{period >= sequence}),
demographic columns, and consent-flow columns.
}
\description{
Produces one row per participant per period, with clusters nested within
sites. Each cluster is assigned a crossover \code{sequence} -- the period in
which it switches from control to intervention -- and every cluster
eventually receives the intervention (the defining feature of a stepped
wedge). The \code{arm} for a given participant-period row is derived from
whether the current \code{period} has reached the cluster's \code{sequence}
(\code{period >= sequence}), so treatment assignment is genuinely driven by
the roll-out schedule rather than an unused column.
}
\examples{
mock_stepped_wedge()
mock_stepped_wedge(n_clusters = 8, n_participants = 400, n_periods = 5, seed = 9)
}
