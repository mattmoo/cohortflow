% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continue.R
\name{continue_criteria}
\alias{continue_criteria}
\title{Continue a criteria pipeline on top of an existing flow}
\usage{
continue_criteria(flow, criteria, join = NULL, by = NULL)
}
\arguments{
\item{flow}{A \code{cf_flow} object produced by \code{\link[=apply_criteria]{apply_criteria()}} (or a
previous call to \code{continue_criteria()}).}

\item{criteria}{A \code{cf_criteria} object with the additional steps to apply.
If empty (\code{length(criteria) == 0}), \code{flow} is returned unchanged.}

\item{join}{Optional data frame of new columns to attach before evaluating
\code{criteria}, e.g. follow-up data collected after the initial screening.
Joined onto \strong{all} of \code{flow}'s original rows (via a left join), so rows
already excluded at an earlier stage also gain the new columns (as \code{NA}
where \code{join} has no match) -- this keeps the full row history intact.
Requires \code{by}.}

\item{by}{Column name(s) shared by \code{flow}'s data and \code{join}, used to join
them. Required, and only used, when \code{join} is supplied.}
}
\value{
A \code{cf_flow} object whose \code{criteria} and \code{steps} are the
concatenation of \code{flow}'s existing pipeline and \code{criteria}, with step
numbers continuing on from \code{flow}.
}
\description{
Applies additional criteria steps to the surviving cohort of an existing
\code{cf_flow}, optionally joining on new columns first. This lets an
attrition pipeline be built up in stages -- e.g. apply screening criteria,
then join newly collected follow-up data and continue with further
criteria -- while keeping a single, continuous step numbering and
denominators that correctly reflect the cohort at each stage (rather than
restarting counts from the original data).
}
\examples{
dat  <- mock_cohortflow(n_participants = 200, seed = 1)
crit <- cf_criteria() |>
  include(~ eligible_screen, label = "Passed screening")
flow <- apply_criteria(dat, crit)

more_crit <- cf_criteria() |>
  include(~ !is.na(consent_date), label = "Consent recorded")
flow2 <- continue_criteria(flow, more_crit)
flow2
}
