% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{as_attrition_tibble}
\alias{as_attrition_tibble}
\title{Build an attrition tibble from a cohort flow object}
\usage{
as_attrition_tibble(
  flow,
  show_categories = TRUE,
  assessed_label = "Assessed for eligibility",
  final_label = "Final cohort",
  digits = 1L,
  branch_by = NULL,
  count_by = NULL,
  group_x = NULL,
  group_y = NULL,
  group_na_label = "Missing",
  levels = character(0)
)
}
\arguments{
\item{flow}{A \code{cf_flow} object produced by \code{\link[=apply_criteria]{apply_criteria()}}.}

\item{show_categories}{Logical. When \code{TRUE} (default) steps with the same
\code{category} are collapsed under a single parent row. When \code{FALSE} one row
per step is produced.}

\item{assessed_label}{Character string for the header row. Default
\code{"Assessed for eligibility"}.}

\item{final_label}{Character string for the trailing final-cohort row.
Default \code{"Final cohort"}.}

\item{digits}{Integer. Number of decimal places for \code{pct_removed}. Default
\code{1}.}

\item{branch_by}{Optional column name (string) for allocation branching.
When \code{NULL} (default), the table has a single \code{n} column unless the
pipeline has a \code{\link[=randomise]{randomise()}} step, in which case its \code{arms} column is
used automatically. When supplied (or defaulted), the surviving cohort
is split by this column and the final row's total is broken out into
one additional column per branch value (named after the branch value,
e.g. \code{Control}, \code{Intervention}), each populated only on the final row.
Use \code{final_label = "Randomised"} when appropriate. Cannot be combined
with \code{group_x}/\code{group_y}.}

\item{count_by}{Optional column name (string) for distinct counts. When
supplied, counts are computed as \code{n_distinct(count_by)} rather than
\code{nrow()}. Essential for crossover data where one participant has several
event rows.}

\item{group_x}{Optional column name (string), present in \code{flow$data}, used
to repeat the attrition table across the "column" direction (e.g. trial
site). See the Grouping section.}

\item{group_y}{Optional column name (string), present in \code{flow$data}, used
to repeat the attrition table across the "row" direction (e.g. time
period). See the Grouping section.}

\item{group_na_label}{Character string used to label the group formed by
\code{NA} values in \code{group_x}/\code{group_y} (see the Grouping section). Default
\code{"Missing"}. Ignored when the corresponding grouping column has no \code{NA}
values.}

\item{levels}{Optional; requires \code{flow} to have been built with
\code{apply_criteria(hierarchy = ...)}. When the default \code{character(0)}, this
argument is ignored and the row-count behaviour above is unchanged. When
\code{NULL}, one stacked block per hierarchy level is returned (finest to
coarsest), each with a \code{level} column and an additional \code{n_consequential}
column (see \code{\link[=apply_criteria]{apply_criteria()}}'s \code{hierarchy} argument for the counting
rule). When a character vector of level name(s), only those levels are
returned, in the order given. Cannot be combined with \code{group_x}/\code{group_y},
\code{branch_by}, or \code{count_by}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{\code{row_type}}{\code{"header"}, \code{"category"}, \code{"step"}, or \code{"final"}}
\item{\code{label}}{Display text for the row}
\item{\code{indent_level}}{\code{0} = header/final, \code{1} = category or top-level
step, \code{2} = sub-step under a category}
\item{\code{n}}{Number of participants at this point (N entering for
category/step rows; N surviving for the final row)}
\item{\code{n_removed}}{Number removed at this step (\code{NA} for header/final)}
\item{\code{pct_removed}}{Percentage removed relative to entering N (\code{NA} for
header; for final row: percentage retained of original N)}
\item{\code{branch}}{Always \code{NA}; retained for backward compatibility}
}
When \code{branch_by} is supplied, one additional column per unique branch
value is appended (named after the branch value), populated only on the
final row with that branch's surviving count. When \code{group_x}/\code{group_y}
are supplied, \code{group_x} and \code{group_y} character columns are appended (see
the Grouping section) and the tibble contains one stacked block per group
combination rather than a single block. When the pipeline has a
\code{\link[=randomise]{randomise()}} step, a \code{post_randomisation} logical column is appended
(\code{TRUE} for header/final/step/category rows at or after that step,
\code{FALSE} before it); this column is omitted entirely when there is no
\code{randomise()} step, so existing pipelines see no schema change.
}
\description{
Produces a flat tibble describing the attrition at each step (or category)
of a cohort flow pipeline. This is the underlying data layer used by
\code{as_attrition_table()}, and is also useful for custom formatting.
}
\section{Row structure}{

The tibble always starts with an "assessed" header row and ends with a
"final cohort" row. When \code{show_categories = TRUE} (the default), steps
that share a \code{category} are collapsed into a parent row (indent level 1)
with individual step sub-rows (indent level 2) nested beneath it.
Uncategorised steps appear at indent level 1 without sub-rows. When
\code{show_categories = FALSE}, one row per step is produced at indent level 1.
}

\section{Percentage columns}{

\itemize{
\item \code{pct_removed}: percentage of entering N removed at this row's step (or
category). For the final cohort row this is the percentage \emph{retained}
relative to the initial N (i.e. \code{n / n_start * 100}).
\item Percentages are relative to \code{n_in} -- the number entering that step or,
for a category row, the number entering the first step in that category.
\item When steps are grouped under a category (\code{show_categories = TRUE}),
each step sub-row's \code{pct_removed} is also expressed relative to the
category's total entering N (rather than its own, progressively
smaller, entering N), so that all rows within a category share the same
denominator and sum consistently with the category row's percentage.
}
}

\section{Grouping (\code{group_x} / \code{group_y})}{

When \code{group_x} and/or \code{group_y} are supplied, the criteria pipeline
stored in \code{flow} is re-applied independently to each subset of the
\emph{original} data defined by the unique values of the grouping column(s)
(e.g. trial site, time period). The returned tibble is a flat, "long"
stack of one full attrition block per group combination, with additional
\code{group_x} and \code{group_y} character columns identifying which subset each
row belongs to (\code{NA} for the ungrouped dimension). This long format is
the input to the grid layout produced by \code{as_attrition_table()}; use it
directly if you want to build your own custom cross-tabulated output.
\code{branch_by}/\code{count_by} cannot currently be combined with grouping.

Rows where the grouping column itself is \code{NA} (e.g. missing ethnicity)
are \strong{not} silently dropped, and are never folded into every other
group's counts. Instead they form their own explicit group, labelled by
\code{group_na_label} (default \code{"Missing"}), so the grouped totals still sum
to the full dataset.
}

\examples{
dat  <- mock_cohortflow(n_participants = 200, seed = 1)
crit <- cf_criteria() |>
  include(~ !is.na(age),        label = "Age recorded",       category = "Valid age") |>
  include(~ age >= 18,          label = "Adults only",         category = "Valid age") |>
  include(~ eligible_screen,    label = "Passed screening",    category = "Eligible at screening") |>
  include(~ !is.na(consent_date), label = "Consent recorded",  category = "Consent") |>
  exclude(~ withdrew,           label = "Withdrew consent", category = "Consent")
flow <- apply_criteria(dat, crit)
as_attrition_tibble(flow)
}
\seealso{
\code{\link[=as_attrition_table]{as_attrition_table()}} for formatted table output.
}
