% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{mock_cohortflow}
\alias{mock_cohortflow}
\title{Generate synthetic cohort data for testing and examples}
\usage{
mock_cohortflow(
  n_participants = 500L,
  n_clusters = 10L,
  n_sites = 3L,
  n_periods = 4L,
  seed = 123L
)
}
\arguments{
\item{n_participants}{Integer. Total number of participant rows.}

\item{n_clusters}{Integer. Number of clusters (e.g., GP practices, schools).}

\item{n_sites}{Integer. Number of sites (clusters are nested in sites;
must be <= \code{n_clusters}).}

\item{n_periods}{Integer. Number of time periods (e.g., waves in a
stepped-wedge design). Set to \code{1} for a simple cross-sectional cohort.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{\code{participant_id}}{Character. Unique participant identifier.}
\item{\code{cluster_id}}{Character. Cluster identifier.}
\item{\code{site_id}}{Character. Site identifier (clusters nested in sites).}
\item{\code{period}}{Integer. Study period (1 = first period).}
\item{\code{sequence}}{Integer. Stepped-wedge sequence the cluster is assigned
to (NA if \code{n_periods == 1}).}
\item{\code{age}}{Numeric. Age in years (some NAs to simulate missing data).}
\item{\code{age_group}}{Character. Age group (\code{"<18"}, \code{"18-39"}, \code{"40-64"}, \code{"65+"}).}
\item{\code{sex}}{Character. \code{"M"} / \code{"F"} / \code{"O"} (other).}
\item{\code{ethnicity}}{Character. One of five broad ethnic groups.}
\item{\code{consent_date}}{Date. Date of consent (NA = did not consent).}
\item{\code{baseline_complete}}{Logical. Whether the baseline assessment is
complete.}
\item{\code{withdrew}}{Logical. Whether the participant withdrew after consent.}
\item{\code{eligible_screen}}{Logical. Whether the participant passed initial
eligibility screening (e.g., diagnosis confirmed).}
}
}
\description{
Produces a tibble that mimics a clustered cohort study with optional
stepped-wedge period structure. The returned data is deliberately imperfect:
some participants have missing values, withdrew consent, or failed screening,
so that inclusion/exclusion criteria have something to remove.
}
\examples{
mock_cohortflow()

# Larger study with three periods
mock_cohortflow(n_participants = 2000, n_clusters = 20, n_periods = 3, seed = 42)
}
