% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{select_within}
\alias{select_within}
\title{Select rows within groups}
\usage{
select_within(criteria, by, predicate, label)
}
\arguments{
\item{criteria}{A \code{cf_criteria} object (or \code{NULL} to start a new one).}

\item{by}{A single column name (string) defining the grouping (e.g.
\code{"participant_id"}).}

\item{predicate}{A one-sided formula evaluated within each group, or a
function that receives a group's rows as a data frame and returns a
logical vector.}

\item{label}{A short human-readable description of this step.}
}
\value{
The updated \code{cf_criteria} object.
}
\description{
The predicate is evaluated separately within each group defined by \code{by} and
must return a logical vector the same length as the group. Rows where the
predicate is \code{FALSE} are dropped and recorded in the excluded rows store.
This is the natural way to select one (or more) records per unit, e.g. the
index operation per patient.
}
\examples{
cf_criteria() |>
  select_within(by = "participant_id",
                ~ consent_date == min(consent_date, na.rm = TRUE),
                label = "Index operation per patient")
}
