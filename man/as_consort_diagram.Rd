% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consort.R
\name{as_consort_diagram}
\alias{as_consort_diagram}
\title{Build a CONSORT flow diagram from a cohort flow object}
\usage{
as_consort_diagram(
  flow,
  show_categories = TRUE,
  assessed_label = "Assessed for eligibility",
  final_label = "Final cohort",
  digits = 1L,
  main_fill = NA,
  excl_fill = NA,
  border_colour = "black",
  border_linewidth = 0.5,
  text_size = 9,
  text_colour = "#222222",
  font_family = "",
  title = NULL,
  title_colour = text_colour,
  wrap_width = NULL,
  big_mark = ",",
  branch_by = NULL,
  stage_by = NULL,
  stage_label_by = NULL,
  count_by = NULL
)
}
\arguments{
\item{flow}{A \code{cf_flow} object produced by \code{\link[=apply_criteria]{apply_criteria()}}.}

\item{show_categories}{Logical. When \code{TRUE} (default) steps with the same
\code{category} are collapsed under a single parent row. When \code{FALSE} one row
per step is produced.}

\item{assessed_label}{Character string for the header row. Default
\code{"Assessed for eligibility"}.}

\item{final_label}{Character string for the trailing final-cohort row.
Default \code{"Final cohort"}.}

\item{digits}{Integer. Number of decimal places for \code{pct_removed}. Default
\code{1}.}

\item{main_fill}{Fill colour for main-flow boxes. Default \code{NA} (no
fill / transparent), keeping the diagram colourless unless a fill is
explicitly supplied.}

\item{excl_fill}{Fill colour for exclusion (side) boxes. Default \code{NA}
(no fill / transparent).}

\item{border_colour}{Border colour for all boxes and arrows. Default
\code{"#000000"} (black).}

\item{border_linewidth}{Line width for all box borders and arrows,
passed on to \code{\link[ggplot2:geom_rect]{ggplot2::geom_rect()}} / \code{\link[ggplot2:geom_segment]{ggplot2::geom_segment()}}.
Default \code{0.5} (the \code{ggplot2} default line width).}

\item{text_size}{Font size (points) for box text. Default \code{9}.}

\item{text_colour}{Colour for all box text (and the title, unless
\code{title_colour} is supplied). Default \code{"#222222"}.}

\item{font_family}{Font family for all box text and the title. Default
\code{""} (the device's default font family).}

\item{title}{Optional plot title drawn above the diagram. Default \code{NULL}
(no title).}

\item{title_colour}{Colour for the title text. Default \code{text_colour}.}

\item{wrap_width}{Optional integer. Maximum character width for wrapping
text in boxes. When \code{NULL} (default), text is not wrapped. When a
positive integer, each line in a box label is wrapped to at most that
many characters using word-boundary wrapping.}

\item{big_mark}{Character string used as the thousands-separator when
formatting counts in box labels (e.g. \code{"1,234"} with the default
\code{","}). Pass \code{""} to disable grouping (e.g. \code{"1234"}).}

\item{branch_by}{Optional column name (string) for allocation branching.
When \code{NULL} (default), the diagram is linear. When supplied, the
surviving cohort is split by this column and branches are drawn below
the final common-flow box, connected via a right-angle "elbow"
connector (a trunk dropping from the final box, a horizontal crossbar,
then a vertical arrow into each branch box). Use
\code{final_label = "Randomised"} when appropriate.}

\item{stage_by}{Optional column name (string) for ordered stages within
each branch (e.g. crossover \code{period}). When \code{NULL} (default), no
stage-specific nodes are drawn.}

\item{stage_label_by}{Optional column name (string) providing labels for
each stage (e.g. crossover \code{arm}). Ignored when \code{stage_by} is \code{NULL}.}

\item{count_by}{Optional column name (string) for distinct counts. When
supplied, counts are computed as \code{n_distinct(count_by)} rather than
\code{nrow()}. Essential for crossover data where one participant has
several event rows.}
}
\value{
A \code{consort_diagram} object (a \code{ggplot2} \code{ggplot}), drawable via
\code{print()}. The underlying box/arrow layout data frames are available
via \code{attr(x, "consort_layout")} for introspection or custom rendering.
}
\description{
Produces a CONSORT-style flow diagram (as used in clinical trial and
cohort study reporting) summarising participant flow through a \code{cf_flow}
pipeline. The main vertical flow shows the number of participants
entering the study and surviving each step (or category, when
\code{show_categories = TRUE}), while exclusions at each step branch off to
the side as a box giving the count, percentage, and (for categorised
steps) the individual exclusion reasons.
}
\details{
This uses the same underlying data layer as \code{\link[=as_attrition_table]{as_attrition_table()}} --
\code{\link[=as_attrition_tibble]{as_attrition_tibble()}} -- so the row structure (header, category, step,
final) is identical across all three output types.

The returned object is a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} plot (with an additional
\code{consort_diagram} class), so it can be drawn with \code{print()}, combined
with \code{+} and further \code{ggplot2} layers/themes, or exported with
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} or by opening an appropriate graphics device (e.g.
\code{\link[grDevices:png]{grDevices::png()}}) before drawing.
}
\examples{
dat  <- mock_cohortflow(n_participants = 200, seed = 1)
crit <- cf_criteria() |>
  include(~ !is.na(age),          label = "Age recorded",    category = "Valid age") |>
  include(~ age >= 18,            label = "Adults only",     category = "Valid age") |>
  include(~ !is.na(consent_date), label = "Consent recorded", category = "Consent") |>
  exclude(~ withdrew,             label = "Withdrew consent", category = "Consent")
flow <- apply_criteria(dat, crit)
as_consort_diagram(flow)
}
\seealso{
\code{\link[=as_attrition_tibble]{as_attrition_tibble()}} for the underlying plain-tibble data
layer, \code{\link[=as_attrition_table]{as_attrition_table()}} for formatted table output.
}
