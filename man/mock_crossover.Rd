% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock-designs.R
\name{mock_crossover}
\alias{mock_crossover}
\title{Generate synthetic data for a crossover trial}
\usage{
mock_crossover(n_participants = 120L, arm_labels = c("A", "B"), seed = 123L)
}
\arguments{
\item{n_participants}{Integer. Number of participants.}

\item{arm_labels}{Character vector. Labels for the treatments being
crossed over. Determines both the number of periods
(\code{length(arm_labels)}) and the number of possible sequences
(all permutations of \code{arm_labels}).}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per participant per period,
including \code{participant_id}, \code{event_id}, \code{period}, \code{sequence}, \code{arm},
demographic columns, and consent-flow columns. Consent-flow columns
(\code{eligible_screen}, \code{consent_date}, \code{baseline_complete}, \code{withdrew})
are constant within a participant across periods, since they describe
trial-level (not period-level) status; \code{withdrew} reflects withdrawal
at any point during the trial.
}
\description{
Produces one row per participant per period. Each participant is assigned
a randomisation \code{sequence} (e.g. \code{"AB"} or \code{"BA"} for a two-period,
two-treatment design), and the \code{arm} in each period is derived from that
sequence -- so participants genuinely switch treatments across periods
rather than \code{arm} being an unused label.
}
\examples{
mock_crossover()
mock_crossover(n_participants = 60, arm_labels = c("Drug", "Placebo"), seed = 3)
}
