% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yaml.R
\name{export_criteria}
\alias{export_criteria}
\title{Serialises a \code{cf_criteria} object to a human-readable YAML file (or string).
The schema is self-describing and can be re-imported with \code{\link[=import_criteria]{import_criteria()}}.}
\usage{
export_criteria(criteria, path = NULL, fn_refs = list())
}
\arguments{
\item{criteria}{A \code{cf_criteria} object.}

\item{path}{A file path to write to. If \code{NULL} (default), the YAML text is
returned as a character string.}

\item{fn_refs}{An optional named list mapping function objects to
\code{"pkg::name"} reference strings.}
}
\value{
Invisibly, the \code{criteria} object (or the YAML string if \code{path = NULL}).
}
\description{
Serialises a \code{cf_criteria} object to a human-readable YAML file (or string).
The schema is self-describing and can be re-imported with \code{\link[=import_criteria]{import_criteria()}}.
}
\section{YAML schema}{


\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{cohortflow_criteria:
  version: 1
  steps:
    - label:  "Adults only"
      type:   include
      kind:   formula
      by:     null
      expr:   "age >= 18"
      fn_ref: null
}\if{html}{\out{</div>}}

Formula-based criteria round-trip cleanly. Anonymous function bodies are
deparsed as a fallback; complex closures may not round-trip and a warning
is issued. Named exported functions can be stored as \code{"pkg::name"} references
via \code{fn_refs}.
}

