% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expected.R
\name{cf_expected}
\alias{cf_expected}
\title{Declare the expected set of observational units}
\usage{
cf_expected(..., stringsAsFactors = FALSE)
}
\arguments{
\item{...}{Named vectors of values, one per grouping variable, e.g.
\verb{participant_id = 1:50, visit = c("baseline", "3mo", "6mo")}. At least
one named argument is required.}

\item{stringsAsFactors}{Passed to \code{\link[=expand.grid]{expand.grid()}}; default \code{FALSE}.}
}
\value{
A tibble with one row per expected combination and one column per
named argument in \code{...}.
}
\description{
Builds a data frame with one row for every combination of the supplied
vectors (a full cross-join), for use as the \code{expected} argument to
\code{\link[=apply_criteria]{apply_criteria()}}. This lets attrition be denominated against the units
that \emph{should} exist (e.g. every participant who was invited) rather than
only the rows actually present in \code{data} -- so a participant who never
appears in the raw data is still counted as failing the first step that
requires non-missing data, instead of being silently absent from the
denominator.
}
\examples{
cf_expected(participant_id = 1:3, visit = c("baseline", "3mo"))
}
