% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{group_include}
\alias{group_include}
\title{Add a group-level inclusion criterion}
\usage{
group_include(criteria, by, predicate, label, category = NULL)
}
\arguments{
\item{criteria}{A \code{cf_criteria} object (or \code{NULL} to start a new one).}

\item{by}{One or more column names (character vector) to group by; supply
more than one for composite (multi-column) grouping.}

\item{predicate}{A one-sided formula using summary functions (\code{n()},
\code{mean()}, \code{n_distinct()}, etc.) or a function that receives a grouped
data frame and returns a tibble with columns \verb{<by>} and \code{.pass}.}

\item{label}{A short human-readable description of this criterion.}

\item{category}{An optional string grouping this step with others for
display. \code{NULL} leaves it uncategorised.}
}
\value{
The updated \code{cf_criteria} object.
}
\description{
The predicate is evaluated in a \code{dplyr::summarise()} context per group
(when a formula) or receives a grouped data frame (when a function). Groups
where the predicate returns \code{TRUE} are kept; all rows in failing groups are
removed.
}
\examples{
cf_criteria() |>
  group_include(by = "cluster_id", ~ n() >= 5, label = "Cluster size >= 5")
}
